FROM python:3.12-slim

WORKDIR /app

# Install only the lightweight deps needed to serve the eel web UI.
# Source is bind-mounted at runtime; no app code is baked in.
COPY requirements.base44.txt .
RUN pip install --no-cache-dir -r requirements.base44.txt

CMD ["python", "jarvis.py"]
